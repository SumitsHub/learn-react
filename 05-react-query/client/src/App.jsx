import { useState } from "react";

function App() {
  const [count, setCount] = useState(0);

  return (
    <section className="section-center">
      <h1>React Query</h1>
    </section>
  );
}

export default App;
